---
version: "2"
linters:
  enable:
    - staticcheck
    - whitespace
  disable:
    - errcheck
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling